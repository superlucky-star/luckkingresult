document.addEventListener("DOMContentLoaded", function () {

const numbers = document.querySelectorAll(".number");

numbers[0].innerHTML = "--";
numbers[1].innerHTML = "--";
numbers[2].innerHTML = "--";
numbers[3].innerHTML = "--";

});
